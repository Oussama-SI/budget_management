<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Scheduled action to update prices daily -->
        <!-- <record id="ir_cron_update_product_prices" model="ir.cron">
            <field name="name">Update Product Prices from Purchase Orders</field>
            <field name="model_id" ref="product.model_product_template"/>
            <field name="state">code</field>
            <field name="code">model.cron_update_prices_from_purchase()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="times_executed">0</field>
            <field name="doall" eval="False"/>
            <field name="active" eval="True"/>
            <field name="nextcall" eval="(datetime.now() + relativedelta(days=1)).replace(hour=2, minute=0, second=0).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record> -->
    </data>
</odoo>
